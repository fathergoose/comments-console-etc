// I actually care quit a lot
var whoCares = 'not me!';

/**
 * Jsdoc style comment do describe a public function
 *
 * @remarsk This is totally fake code
 *
 */

export const dummyFunction = () => {
  // TODO: Change to "wassup"
  // Multi-line single line comment
  /* if (process.env['ENVIRONMENT']) {
  /*   callFunction()
  /* } else {
  /*   return 0
  */
  console.log(whoCares, 'helloooooo');
};
